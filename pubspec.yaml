name: disrupton_app
description: Aplicación de realidad aumentada para exploración cultural

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.17.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  
  # Realidad Aumentada
  ar_flutter_plugin_updated: ^0.0.1
  # Estado y navegación
  provider: ^6.0.5
  go_router: ^10.1.2
  
  vector_math: ^2.1.4 
  # HTTP y API
  http: ^1.1.0
  dio: ^5.3.2
  
  # Almacenamiento local
  shared_preferences: ^2.2.2
  path_provider: ^2.1.1
  
  # Cámara y permisos
  camera: ^0.10.5+5
  permission_handler: ^11.3.1
  
  # UI y animaciones
  cupertino_icons: ^1.0.2
  lottie: ^2.7.0
  
  # Utilidades
  intl: ^0.18.1
  url_launcher: ^6.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  
  assets:
    - assets/images/
    - assets/models/
    - assets/animations/
  
  # fonts:
  #   - family: Roboto
  #     fonts:
  #       - asset: assets/fonts/Roboto-Regular.ttf
  #       - asset: assets/fonts/Roboto-Bold.ttf
  #         weight: 700
